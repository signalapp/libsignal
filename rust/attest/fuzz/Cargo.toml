
[package]
name = "attest-fuzz"
version = "0.0.0"
authors = ["Automatically generated"]
publish = false
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
attest = { path = "../" }

# This isn't used directly, but v1.4.0 has a bug that breaks compilation for
# older Rust versions. Once https://github.com/rust-fuzz/arbitrary/issues/202 is
# resolved and a new version is released we can remove this dependency.
arbitrary = "~1.3"
libfuzzer-sys = "0.4"

# Prevent this from interfering with workspaces
[workspace]
members = ["."]

[[bin]]
name = "dcap"
path = "fuzz_targets/dcap.rs"
test = false
doc = false
