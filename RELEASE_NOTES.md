v0.96.5

- Enforce a minimum TCB level for all SGX attestations.
- Add setUsernameLink() typed API
- RegistrationService now also refreshes its cached session state from rate-limited (429) server responses.
